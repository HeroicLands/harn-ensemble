---
"harn-ensemble": patch
---

**This collection moves to `@heroiclands/package-build@^21.2.0` and
`@heroiclands/hugo-theme@^0.6.0`.** The two travel together: the toolchain emits
the infobox data the theme's generic renderer draws.

Every labelled link between character notes resolves, so a reference from one
dossier to another lands on the right page. Artwork is addressed by the package
that ships it, which is the form these notes already write.
