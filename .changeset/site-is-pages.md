---
"harn-ensemble": patch
---

**The package's homepage is its site homepage**, published at `/harnensemble/`. Each NPC is a page of its own: `being-<id>/` and `affiliation-<id>/`. **Generated section and tag listings removed** — `affiliation/` and `being/` section landings, `tags/` and its tag pages, and the `homepage-root/` redirect are all gone.
